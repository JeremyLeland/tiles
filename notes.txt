3/16:

Creating my own tiles is a pain in the ass, but so is using other people's tiles

The ones I create myself are the easiest to use

The ones created by others look good, but require lots of massaging to try to get to display the way I need them to
The end result hasn't looked that good, anyway

Gonna try making my own version of the roadTiles

Setting the transforms is kind of a neat trick to let me use rects, but it's making it harder to do height now
  - Easier to just manually specify the diagonals?
  - What's the trick to increasing/decreasing height?
  - Can I change transform mid path?
    - Yes you can!
    - Though it seems like the math is just adding the amount you want to go down to both x and y

Can I define the top of the tiles in 2D (of sorts) and use transforms to get the 4 different views?
  - Could similarly define all 4 sides and pick the two that are visible (and appropriately transform them)
  - Need different "go down" transforms for these
  - Can't change transform in middle of path (maybe it's for the best)

Future optimization: only drawing visible left/right side surfaces


3/15:

Since the desert tiles all have 4 directions, can rotate around map to show 4 different angles

3/14:

Start experimenting with tiles again, using kenney's collections for inspiration

NEXT: Create python script to combine separate tiles into one image, with a json guide for name, direction, and sx,sy,sw,sh

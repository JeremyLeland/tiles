8/13:
  - DONE: Move playing left or right, then move them up to the closest floor
  - In arcade style, let player move left or right while in the air
    - It doesn't make any sense, but it feels wrong if you can't
  - What about my earlier method of checking every spot in the half-circle?
    - This should give me the first empty spot
    - We could get a slope from bottom to first empty spot and travel along that?
  - Floor finding works pretty well
    - Need to prevent us from walking sideways through walls (or ceiling!)
    - Should prevent us from moving up too steep of a slope

8/12:
  - DONE: Shoot projectiles
  - DONE: Have them take out circles when hit wall
    - It even stays near 60 fps on my laptop
  - Can I use something similar to move the player?
    - Something like getMapHit...check points around bottom, see which ones are open
      - If one is open within that bottom arc, move player in that direction?
        - No, that's all wrong
  - Keep it real simple to start, ignore our sides
    - Find the floor under us
      - Check height of next column over (left or right)
        - If that height is less than X pixels different (maybe 2 for now), move us up to that height  

8/11:
  - DONE: Did line versus level test
  - DONE: How can I make this sweep for larger objects?
    - Advance the edge of a circle?
      - Check the center point + the radius from angle -PI/2 to angle + PI/2?
      - How do I know how many points to check?
        - Find out what the angle distance is per pixel
        - Or, just do 10 of them for now?
  - NEXT: Shoot projectiles, have them take out circles when they hit wall

8/10:
  - Interactive carving of circles
    - Can re-use existing maskData and keep calling putImageData
      - Does dirty rect help us here?
      - Probably makes things more complicated if we ever need to update several parts of map at once
  - Map representation
    - Uint8Array? I think I just need 0-empty, 1-diggable, 2-solid
    - Easy way to turn Uint8Array into base64 encode and back? Better than 320x240 of numbers?
    - Modify map and then update mask as part of the change?
      - How slow would it be to putImageData data every frame?
        - rabbit hole danger
        - Also probably not that hard to update them both as changes are made
        - But if lots of changes are happening at once, then "every frame" becomes the base case
        - Can see if it's noticeable on laptop
  - Looking at a Level object
    - lets us implement toJSON(), which is something I should've been using much earlier on
    - need to make some decisions on where mask image goes and how that gets kept in sync
    - Ideally, I feel like Level shouldn't know anything about drawing
    - Maybe it can have an updateMaskImage() function that takes in the mask data object?
    - Test this separately from my maskMap test, since it's breaking things
  - Forget all that. Don't worry about base64 for now, just save it as a big array. Who cares?
    - Would only be an issue if storage size became an issue. No one actually has to look at this right now.


8/9:
  - Generate fractal clouds for layers instead of all those circles?
    - Since I'm doing a pixelated image anyway, can render noise per pixel
  - Interactive carving of circles (like the last editor)
  - Move character around screen and shoot projectiles that will make tiny circles on detected impact
    - Will need some sort of collision test (sweep eventually?)

8/6:
  - 2x2 could be fun to play with at higher resolutions
    - 32x32 could work for players same size as tile
      - If players smaler than tile, maybe get into weirder collision situations
    - Try 64x64 grid with larger circle cursor
    - Show cursor as outline
    - Scroll wheel to change cursor size
  - Going down a performance rabbit hole on old laptop
    - Stroke is definitely slow
      - 30fps to 60fps taking out strokes and just doing fills
    - All the fills is still a lot
      - 64x64 is 40fps
      - Tried premaking the paths, but that didn't help at all
        - Actually, seemed even slower on 64x64!
        - Should probably make this toggleable to check for sure (and to avoid all the commenting out of code)
      - Haven't successfully tried combining into one huge fill() operation vs smaller fill operations
        - Could experiment!
          - Also made things slow
        - Could also special-case the solid square to be just a rect
      - Ideally, all these should be toggle-able for easier testing on same map (and avoiding commenting out code)

  - Other tile ideas
    - Square broken into 4 triangles? Tetrakis -- https://en.wikipedia.org/wiki/Tetrakis_square_tiling
      - This can support 45 degree lines as well as horiz and vertical
      - Not sure if that ends up being much better than what I have now


  - Trying to stay focused on this being used for something
    - Something Liero-like?
    - Add a box that can move around on ground and shoot projectiles
      - Maybe give it a jetpack, too -- easier than grappling hook for now
    - Projectiles should take out part of the wall
    - This can be test of different map infrastructure ideas

  - Maybe just do actual Liero
    - Pixels instead of blended tiles. Make it lower resolution and scale it to fill canvas.
    - "Draw" circles of explosions onto it
    - Render dirt image with a mask of the level
    - Have a box for character for now that goes around level
    - Play with what height to place box at, when it should climb, when it should be stopped


8/5:
  - Making version of gridPaint for 3x3 tiles
    - Actually worked pretty well on first attempt
    - Isn't reacting to missing N, W, S, E
      - I can make a diamond by taking out corners, but not the inverse by taking out edges
      - Issue with how I'm drawing shape, probably
      - Got convex behaving better, but still issues around 0-index boundary
        - Maybe wait until I find firstIndex to start the actual drawing?
  - Dang, now we've broken the basic diamond case
    - Re-think that with new method
    - Need to know if we're in concave mode or convex mode?
      - If you reverse the 1s and 0s, you should inverse the shape
      - What if the unused middle bit was used to determine drawing method?
        - If center is solid, give us a middle shape
        - If center is empty, draw separate corners?
    - Or is it that empty corners mean something different than empty edges?
  - Seems like the first way is simplest, I got to into the weeds on what 3x3 shapes should look like
  - Regardless, 2x2 is easier to deal with. 3x3 always feels awkward.
    - Not sure how useful 2x2 ends up being, though
      - Gets back to me not having a real use case for this yet, beyond a vague idea of 'ant farm'
  - Next step could be a gridPaint with regular triangles (hexes broken into triangles)
    - Align so that you could have flat horizontal and then slopes
      - Doesn't allow vertical walls, but might still look interesting
  - Honestly, maybe I just need something to manually cut holes
    - Use mouse to "nudge" points away?
      - Have points try to stay away from each other so the line segments are somewhat uniform?

8/4:
  - 9 bits for corners has weird/invalid cases, too
    - Two parallel collinear lines created with no space between them?
    - Make a table of all possible tiles (like we did for the other version)
      - Oh man, there's a ton of tiles that don't make any sense
      - Might be easier to try to make the other editor work than deal with all these cases
  - First way
    - Maybe we automatically 'clean up' non-sensical tiles after every placement?
      - Not sure if the fix is easy to determine programatically
    - Or maybe we just make the human do it
    - NOW: Support clicking and dragging so we can at least get a sense of how it looks
      - Move much of pointerDown into pointerMove
      - Could choose to leave hard edges
        - This creates case where a tile has more than 1 line
        - Not sure the constraint ends up being all that useful
        - Maybe we just want a nicer way to "carve out" a shape from a solid as a bunch of lines?
  - It's feeling like these constraints aren't super useful
    - I'm looking for a way to "dig" out of a solid, like an ant colony
    - Maybe a better way to think of this is to start with a simple pixel grid based digger
      - Just click and drag around to make things solid or empty
      - Then apply differet ways of interpretting that pattern
        - Direct boxes
        - corners
        - corners and edges
      - See if any of those make sense or look good
      - Clearly things *can* be represented with these tiles
        - I think the question I'm asking is does this make things any easiser to create/edit/deal with?
          - Potentially makes easier to draw without knowing entire loop, that's all I have so far!
          - I haven't even tried using these to collision detect/navigate yet

8/3:
  - There are lots of invalid cases that come up while dragging the mouse around this way
    - For instance, click at 1,1 and 2,2
    - This would result in a tile with two walls
    - To avoid this, we have to backtrack and further shift walls
      - We certainly can make user do it manually
        - You can still make good looking shapes this way
        - You just can't do everything
      - It requires altering several tiles around it
        - not sure how messy it will get to figure this out automatically
        - It's like rasterizing a line
  - The question is how useful is the constraint of one wall per tile
    - Does this end up being that much more useful than a bunch of lines, with or without loop info?
  - NOW: Another possibility
    - Bits to define which corners/edges are solid?
      - 9 bits for corners, edges, middle
      - Use mouse to turn these off and on
      - Bits shared by adjacent tiles? More like how we did our partial tile blending before
        - But with an extra col/row, so we can do 30/60 degree slants
        - The sharing might make things weirder
          - If we don't share, we can have more options with slants on one side and cut offs on other
          - Just need to treat the edges of a triangle as solid too, not just the slope
          - er, wait...how are we representing a slant across?
            - maybe we do need the far end
        - The middle is kind of a spoiler
          - Could just ignore it if it looks bad
          - Leave it for now just to try the thing out.
    - Key is that the bits represent corners and edges (same as we were indexing before), not a 3x3 grid inside
      - This makes it more clear that side bits are shared
      - Probably need to ignore the middle, since it doesn't represent an edge or corner
      - Gives us some potentially weird blocks (like all corners/edges except one?)
        - We can sketch these out in a demo and see what they look like
    - Draw start point and stop points for each tile
      - Go in circle to find first off point. This is start.
      - Continue circle until next off point. This is stop.
      - Draw green and red dots for proof of concept before trying lines.

8/2:
  - What happens when we click on an edge midpoint?
    - DONE: Nudge existing points outward? Away from current gap?
  - BUG: Making a big diamond and clicking in the same corner twice messes up
    - Click top corner, center, then right corner twice. Messes up NW corner.
    - Lots of errors when extending a shape
      - Cases where more tiles need to be updated?
      - Cases where tiles should be changed to completely empty?

7/30:
  - DONE: Even out the horizontal on both sides
  - Technique works for horiz and vertical, pulled out into helper function
  - NEXT: Clicking at mid edges, should make a 30 degree wall instead of 45
  
7/29:
  - DONE: Place small diamond in empty corner
  - If two diamonds are placed next to each other, need to properly join them
  - Look at existing start/end points, incorporate those?
    - min/max sometimes works, but this seems fragile. Need to account for wrapping at 7?

7/28:
  - Which points would be affected by a given spot?
    - DONE: Show closest spot on grid near mouse
    - Say which points would be moved?
  - What if I make a diamond around every intersection?
    - Move whichever points are necessary to do this
    - Then figure out the cases of midpoints on edges

7/23:
  - Adjust an edge up when I click
    - Move right tile's point clockwise (index goes down)
    - Move left tile's point counter-clockwise (index goes up)

  - Can we determine what start/end points are near mouse and nudge them out of the way?
    - On a filled in corner, this should create a minimal offset (small diamond)
    - If click near a middle, if has middle index it should move
      - Don't need to move everything on side, just the ones that join near that point?

7/22:
  - Using this project to experiment with partial edge tiles
    - A square has 8 points at corners and edges
      - these tiles are made up of every possible line between these points
        - I'm sure there's a name for this, but I don't want to ruin this by looking it up
      - Can get some more interesting slopes, still keep it constrained
        - So I think there are 36 viable titles here, out of 64 combinations
          - 4 border edges could potentiall be collapsed, if treated as all walls solid
          - 4 corners
          - 4 30-60-90's one way
          - 4 30-60-90's other way
          - 4 halfs
          - 4 diagonals
          - 8 opposite 30-60-90's
          - 4 opposite corners
        - Even if only half of them are viable, I still think this makes sense as a way to organize them
          - If I include edges with no fill, it's 40
            - valid if abs( startIndex - endIndex ) >= 2 or <= 6
            - not sure I'd draw these, but they are valid
            - this focuses on edges, rather than fill

  - Can play with this here so I'm not making a new project

  - Ideas for editing a map like this
    - Click on intersection to form smallest opening (small diamond)
    - Drag along grid lines to move startIndex in that direction?
      - Small diamond widens in dragged axis?
    - Hover over closest edge and show the applicable start/end points on that edge
      - Edit both start/end points by clicking and dragging?

4/14:

Thinking about games I actually want to play that I could make
  - Brotato is something I keep coming back to
  - Simple graphics
    - just one side image that gets squished up and down, I think
    - and flipped depending on direction?
  - Simple arena
    - don't need to design any levels, really
    - could add some cover if I want, maybe
  - Simple controls
    - since we auto-fire, could play on tablet without mouse
  - This would be yet another case for smooth movement of crowds
    - avoiding each other (plus maybe obstacles?)
    - swarming player (if melee, or firing from distance)
    - I feel like I keep re-doing this, so I may get annoying deja vu
      - but I also feel like I've never actually gotten it

Could use Kenny monster builder pack
  - Individual limbs and bodies to mix and match
    - Potential rabit hole, though could be funny to automatically mix and match as experiment
    - May need to determine attachment point for each limb to animate around, which could be slog

Can probably find still images of front facing monsters
  - Maybe rotate these side to side as they move to player?
  - Side facing would squish up and down

Land Monster
  - Maybe use "Land Monsters" from https://opengameart.org/content/monster-cc0

4/13:

- Still want to experiment with metaballs below
- Also want to try varient: metalines
  - Use to model vein-y leaves? Oak, maybe?
  - Line for main vein, then lines for branches
  - Distance from lines determines color (or transparent)

Ok, maybe fuck hex stuff for now. This is way more effort than I actually care about for grass texture
  - I'm not even making a hex game here
  - Some resources for later:
    - https://www.redblobgames.com/grids/hexagons/
    - https://observablehq.com/@jrus/hexround

Messed with metaballs
  - Kind of a bright mess
  - If I want to blend between motes, I already have code for joining two circles with another curve
    - All the road intersection shit
    - I could just use that to make a blend between the circles at their intersection point
      - and draw it in 2D

4/9:

- Shader experiment: 2D Metaballs for Motes (Osmos clone)
  - https://en.wikipedia.org/wiki/Metaballs
  - Not sure how this would actually look in game, but for kicks
  - See how a bunch of metaballs of different sizes look as they bounce around a square
  - Send list of balls to shader, have it iterate through them to render blended balls
  - Does it look good? Is it performant?
  - This would mostly be for the effect of one mote eating another
    - Could have the color be brighter when its part of two motes as well as having the shape
  - Do we need to render entire area like this?
    - Maybe we could send a rect that is just the balls involved in collision?
      - That's a later optimisation
      - For kicks, see how it goes with a giant rect for all of them for now

4/2:

- Shaders: hexagon pattern (for fun/practice) vs worley
  - Started something
  - I think I figured out most of this a while ago with some hex tests
    - Was it the Tantrix code? That had hexagonal stuff
  - Need to turn a given point into a hex cell coordinate
    - So I can give each hexagon a consistent color

- Shaders: anti-aliasing? (average out nearby points?)
  - Trying super sampling
    - seems like this will be less useful further away
    - would want to sample more things?
  - Would it be easier to generate the texture once with shader?
    - then use the regular mip mapping and whatnot to sample down?
  - Got something that looks pretty good with fwidth and smoothstep
    - https://shadergif.com/guides/anti-aliasing-basics/

- Move some common code out of simpleShaders so we can make more of these and be shorter
  - Move camera mouse input code to OrbitCamera?
  - Move grid to its own thing? Grid helper, axes helper of some sort?
  - Use Plane helper instead of creating my own?
  - Shader hash functions into ShaderCommon?

- Add grass clumps to grass tiles (as a combined demo)

I'm having all these problems with distance
  - Can I avoid all that with orthographic projection?
    - And no zooming. Keeps everything at predictable level of detail.

4/1:

Shaders: Distance from point
  - Start with basic: distance from center of each cell
  - Random offsets per cell, check neighbors
    - Worley is the name for this

Grass idea:
  - Like worley, but with solid colors -- stained glass look?

Water idea:
  - Like worley, but with dark blue center and bright white borders (to be disturbances in water)
  - Have the points move?
    - Either slowly around mostly in place for a stable body of water, or...
    - Have them all moving in one direction, as well as somewhat up and down, for a river?

Made some flowers

The textures all look crappy when I zoom out
  - They get grainy and pixelated, rather than nicely blurring
    - Can I do this sampling myself?
    - Or is it easier to just use textures

3/31:

DONE: Blades of grass
  - Removed extra vertices, since these are all part of same surface

GLSL syntax highlighting in javascript string literals
  - I need the glsl-literal extension
  - For some reason, this isn't available through OSS Code marketplace
  - Was able to download VSIX through VS Code proper on my laptop
    - Saved to my conf folder in website for future
    - This is dependent on "Shader languages support for VS Code" (slevesque.shader)
      - ...which is available through OSS Code, fortunately

NOW: Play with shaders
  - DONE: Simple rectangle to try shaders on
  - Squares
  - Trianges (split squares diagonally)
  - Triangles equilatoral?

Shader ideas
  - Do the 'distance from point' thing
    - Rocks, scales
  - List of points? Point in each cell, check against this and adjacent ones
    - Then we can generate based on wherever
  - This should give us a Voroni map, which we could fill with solid colors instead of gradients
    - That might look nice for grass
  - Can also make my little flowers from before, maybe at a random point in each cell

3/30:

TODAY:
  - Blades of grass (geometry)
  - Texture test with repeating triangles (in hexagon shape?)

DONE: Blades
  - Got some I'm decently happy with
  - Normals are tricky, I think I have them mostly correct
    - Looks decent with the Lighting shader

I created planes for each section of curve, but I don't need to do that!
  - Need to duplicate when same point is part of different surfaces with different normals
  - In this case, it's all the same curve and sharing normals
  - So I should be able to take out extras!


3/29:

DONE: Add/Delete cols and rows with c and r keys
  - How can I resize the grid when I do this?

Someday: trees (but the normals and merging for these could be tricky)

Sooner: clumps of grass, flowers
  - Foxtail like plants are a steam and colorful cone
    - Simulate this with two planes, each plane having a sawtooth-ish color texture/shader?
  - Grass clump
    - Two planes with a series of curves?
    - Or would it be fast enough to just do these with actual geometry?
      - Maybe do that first -- I feel like the curve calculations are going to be slow
  - All of this should get a billboard test runner, a quick template I can put shaders into to try out

Rocks/boulders
  - Perturbed spheres?
  - Or generate them in fractal-y way?
    - Big general shape with a few polygons, then add detail to these?

Tiles:
  - Sand, paths?
  - Water -> Sand -> Path -> Grass?

NEXT: Grass
  - I have the clearest idea of how to do this, so start here
  - Several curved blades of grass in a circle

TIDY:
  - Should have GLGameCanvas track mouse state like GameCanvas does

3/26:

Use mouse input for editing grass/water tiles in 3D
  - Just the flat plane for now

View matrix can live in camera
Proj matrix live in GLGameCanvas so we can convert click into clip space?
  - For flexibility, maybe the rest of the process of applying view matrix will have to happen in caller

DONE above

Maybe add buttons for duplicating/deleting rows and cols to make bigger?

3/25:

Merged branch, since I'm going to focus on WebGL for now

NOW: Experiment with additional layers/levels for elevation
  - Draw blocks without water if above ground level
  - Eventually figure out some blending downward

Need a test that will draw the tiles directly for now
  - so I can see how the slopes look until I figure out the blending

LATER: Keep level flat, add details to grass and water?
  - Flowers, tufts of grass, lillypads, reeds
  - Bridge over water (cylinder logs)

3/24:

DONE: Maintain viewport ratio as we resize window (showing extra on edges if needed)

DONE: Add OrbitCamera in (to help verify angles we can't otherwise see)

DONE: Break simpleWebGL.js into smaller pieces

NEXT: Game of Life to create map?
NEXT: Slopes to allow higher ground? How would that blend?
NEXT: Detect where mouse is pointing to in world?
  - This would be most useful, since I don't have an example of this anywhere else
  - Going to involve shooting a ray at a plane, I think
  - Detect where we are in projection plane, then apply the inverse of the view matrix? Or something...


3/23:

Got it drawing all the NW/NE/SW/SE variations of grass and water

Tried blending tiles from array.
  - This seemed like it made sense before, but is giving me weird results now
  - Are my grass tiles too thin?

DONE: What I really need is a grid to make it clear which blend tiles are contributing to which actual result
  - Make something to draw lines...

Translated via keys

Turns out everything gets stretched weird when window resizes
  - I need to adapt what I was doing with my minimum bounds for canvas view
  - Resizing window should maintain aspect ratio and show more or less around edges

3/18:

Made WebGL version today
  - Was able to leverage code from Kingdoms to get something running pretty quickly
  - Not that hard to build the mesh with curve
    - Not much worse than creating the 2D coordinates for the SVG, anyway
  - With 10 segments, looks basically as good as canvas curve
  - Seamless (as opposed to missing edges when drawing with canvas)

May as well keep pushing with this and turn this into another WebGL experiment
  - Gives a chance to finish up the lighting shader work from before
  - A chance to mess with shader textures some more, too

Array of which mesh and which rotation go with each NW/NE/SW/SE combination?

3/17:

Experiment with 3D points drawn with 2D methods
  - Use matrices to do this in new 3D branch

So I got this to work with a simple pond example,
  - but I'm going to want backface culling and z-ordering soon, probably
  - The curves are theoretically nice,
    - but it's probably better to just generate a curve with lots of smaller triangles
  - If I'm going to all this effort, I may as well just use WebGL proper
    - Maybe it's time to take another look at that

3/16:

Creating my own tiles is a pain in the ass, but so is using other people's tiles

The ones I create myself are the easiest to use

The ones created by others look good, but require lots of massaging to try to get to display the way I need them to
The end result hasn't looked that good, anyway

Gonna try making my own version of the roadTiles

Setting the transforms is kind of a neat trick to let me use rects, but it's making it harder to do height now
  - Easier to just manually specify the diagonals?
  - What's the trick to increasing/decreasing height?
  - Can I change transform mid path?
    - Yes you can!
    - Though it seems like the math is just adding the amount you want to go down to both x and y

Can I define the top of the tiles in 2D (of sorts) and use transforms to get the 4 different views?
  - Could similarly define all 4 sides and pick the two that are visible (and appropriately transform them)
  - Need different "go down" transforms for these
  - Can't change transform in middle of path (maybe it's for the best)

Future optimization: only drawing visible left/right side surfaces

Would it be easier to just store all these things as 3D points and convert them to 2D with a projection matrix?
  - Just do the 3D math?
  - Rather than trying to keep track of directions and have different functions for the sides and whatnot
  - Just define everything in 3D space and then project it
  - Make this a different branch


3/15:

Since the desert tiles all have 4 directions, can rotate around map to show 4 different angles

3/14:

Start experimenting with tiles again, using kenney's collections for inspiration

NEXT: Create python script to combine separate tiles into one image, with a json guide for name, direction, and sx,sy,sw,sh
